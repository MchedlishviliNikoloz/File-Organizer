# 📁 File Organizer

მარტივი და ძლიერი პროგრამა, რომელიც ავტომატურად ალაგებს ფაილებს ნებისმიერ ფოლდერში წინასწარ განსაზღვრული, ფაილის ტიპის ან შენი საკუთარი კონფიგურაციის მიხედვით — ასევე აქვს დუბლიკატების პოვნის და undo ფუნქცია. ხელმისაწვდომია როგორც კონსოლ, ასევე გრაფიკული ინტერფეისით.

🇬🇪 ქართული | 🇺🇸 [English](README.md)

---

## ✨ ფუნქციონალი

- 📂 **ნაგულისხმევი კატეგორიზაცია** — ფაილები დალაგება წინასწარ განსაზღვრულ კატეგორიებში (სურათები, დოკუმენტები, აუდიო, ვიდეო, კოდი, არქივები)
- 🔤 **გაფართოებით კატეგორიზაცია** — ფაილები იჯგუფება გაფართოების მიხედვით (`.jpg`, `.pdf` და სხვა)
- ⚙️ **კონფიგურაციის მართვა** — შეგიძლია შეცვალო და გააფართოო წინასწარ განსაზღვრული წესები
- 👤 **პირადი კონფიგურაცია** — შექმენი სრულიად საკუთარი კონფიგურაცია
- 🔍 **დუბლიკატების პოვნა** — MD5 hash-ით პოულობს და გადაიტანს იდენტური კონტენტის ფაილებს
- ↩️ **გაუქმება (Undo)** — ნებისმიერ დროს შეგიძლია გააუქმო ბოლო ორგანიზება
- 📊 **Progress bar** — რეალურ დროში გიჩვენებს გადატანის პროგრესს
- 📝 **ლოგირება** — ყველა ოპერაცია იწერება `organizer.log` ფაილში
- 🔄 **საწყის კონფიგურაციაზე დაბრუნება** — ნებისმიერ დროს შეგიძლია აღადგინო ორიგინალი კონფიგურაცია
- 🖥️ **გრაფიკული ინტერფეისი** — თანამედროვე dark-themed GUI customtkinter-ით

---

## 📋 მოთხოვნები

- Python 3.10+
- tqdm
- customtkinter

---

## 🚀 ინსტალაცია

**1. გადმოწერე პროექტი:**
```bash
git clone https://github.com/MchedlishviliNikoloz/file-organizer.git
cd file-organizer
```

**2. დააინსტალირე dependencies:**
```bash
pip install tqdm customtkinter
```

**3. გაუშვი პროგრამა:**

კონსოლ ვერსია:
```bash
python main.py
```

GUI ვერსია:
```bash
python gui.py
```

---

## 📖 გამოყენება

### კონსოლ ვერსია

გაუშვი პროგრამა და შეიყვანე ფოლდერის მისამართი:

```
📂 Enter folder path: /Users/username/Desktop/MyFolder

1. Organize by default categories
2. Organize by file type
3. Organize by my categories
4. Find and move duplicates
5. Manage default categories
6. Manage my categories
7. Undo last organize
```

### GUI ვერსია

გაუშვი `gui.py` გრაფიკული ინტერფეისის გასახსნელად. აპლიკაციას აქვს სამი მთავარი სექცია:

- **Dashboard** — აირჩიე ფოლდერი, ორგანიზების რეჟიმი და გაუშვი ოპერაცია
- **Core Config** — ნახე და შეცვალე ნაგულისხმევი კატეგორიების კონფიგურაცია
- **My Layouts** — შექმენი და მართე შენი პირადი კონფიგურაცია

---

### ნაგულისხმევი კატეგორიები

ფაილების ორგანიზება შემდეგ ფოლდერებში:

| კატეგორია | გაფართოებები |
|-----------|-------------|
| 📷 images | .jpg, .jpeg, .png, .gif, .webp, .svg, .bmp ... |
| 📄 documents | .pdf, .docx, .txt, .xlsx, .pptx, .md ... |
| 🎵 audio | .mp3, .wav, .flac, .aac, .ogg ... |
| 🎬 video | .mp4, .mov, .avi, .mkv, .wmv ... |
| 💻 code | .py, .js, .html, .css, .ts, .json ... |
| 🗜️ archives | .zip, .rar, .7z, .tar, .gz |
| 📦 other | ყველა დანარჩენი |

### უკან დაბრუნება (Undo)

ორგანიზების შემდეგ ნებისმიერ დროს შეგიძლია გააუქმო ბოლო ოპერაცია — აირჩიე **7. Undo last organize**. ისტორია ინახება `.undo_log.json` ფაილში.

### დუბლიკატების პოვნა

პროგრამა სკანირებს ფაილებს **MD5 hash**-ის გამოყენებით და პოულობს იდენტური კონტენტის ფაილებს (სახელის მიუხედავად). დუბლიკატები გადავა `duplicates/` ფოლდერში, ორიგინალი ფაილი კი თავის ადგილზე რჩება.

---

## 🗂 პროექტის სტრუქტურა

```
file-organizer/
│
├── main.py              # კონსოლ ვერსიის გამშვები ფაილი
├── gui.py               # გრაფიკული ინტერფეისი
├── organizer.py         # მთავარი ლოგიკა
├── config_manager.py    # კატეგორიების მართვა
├── utils.py             # დამხმარე ფუნქციები
├── logger.py            # ლოგირების კონფიგურაცია
│
├── config.json          # ნაგულისხმევი კონფიგურაცია
├── config.default.json  # კონფიგურაციის სარეზერვო ასლი
├── user_config.json     # მომხმარებლის პირადი კონფიგურაცია
│
└── README.md
```

---

## 📄 პროგრამის მიერ შექმნილი ფაილები

ორგანიზების შემდეგ ფოლდერში შეიძლება გამოჩნდეს:

| ფაილი | აღწერა |
|-------|--------|
| `organizer.log` | ყველა ოპერაციის ლოგი |
| `.undo_log.json` | უკან დაბრუნების ისტორია (იშლება undo-ს შემდეგ) |
| `README.txt` | შექმნილი ფაილების აღწერა |

---

## 🛠 გამოყენებული ტექნოლოგიები

- [Python](https://www.python.org/)
- [tqdm](https://github.com/tqdm/tqdm)
- [customtkinter](https://github.com/TomSchimansky/CustomTkinter)